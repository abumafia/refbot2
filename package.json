{
  "name": "refbot2",
  "version": "1.0.0",
  "description": "Telegram Referral Bot with MongoDB",
  "main": "bot.js",
  "scripts": {
    "start": "node server.js & node bot.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"node server.js\" \"node bot.js\""
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^5.1.0",
    "mongodb": "^5.7.0",
    "telegraf": "^4.12.2"
  },
  "engines": {
    "node": ">=18.x"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
